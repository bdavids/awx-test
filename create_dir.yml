#
# Create backup directory
#
---
- name: "Create dir : {{ bck_cfg_dir }}"
  file:
   path:  "{{ bck_cfg_dir }}"
   state: directory
  run_once: true
  changed_when: no # Don't report changes
  delegate_to: localhost
